{{ attach_library('module_hero/hero-style') }}
<h3 class="animated bounce">{{ title }}</h3>
<h4>{{ subtitle }}</h4>
<ol>
    {% for item in items %}
        <li class="animated {{ random(['pulse', 'swing', 'jello']) }}">{{ item.name }}</li>
    {% endfor %}
</ol>